<div class="wrapper"> 
    <% unless logged_in? %>
        <div> <%= link_to "Login",  user_login_path, class: 'btn btn-primary btn-login' %> </div>
        <div> <%= link_to "SignUp", new_user_path, class: 'btn btn-primary btn-login' %> </div> 
    <% end %>
    <% if logged_in? %>
        <h3> Welcome Here!</h3>     
    <% end %>

 
</div>

<style>
.wrapper {
    margin: 200px;
    text-align: center;
}
.btn-login {
    width: 400px;
    padding: 20px;
    margin-top: 20px;
    font-size: 30px;
}
</style>