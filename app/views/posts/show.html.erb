<h1><%= @post.title %></h1>
<p><%= @post.description %></p>
<ul>
    <li><%= link_to "Edit", edit_post_path(@post) %></li>
    <li><%= link_to "Destory", post_path(@post), data: {
        turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
    </li>
</ul>