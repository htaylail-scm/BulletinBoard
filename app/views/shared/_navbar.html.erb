<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "SCM Bulletin Board", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if admin? %>
            <%= link_to "Users", users_path, class: "nav-link" %>
          <% end %>        
        </li>
       
        <li class="nav-item">
          <% if logged_in? %>
            <%= link_to "User", user_profile_path(current_user.id), class:  "nav-link" %>
          <% end %>
        </li>
        <li class="nav-item">
            <%= link_to "Posts", posts_path, class: "nav-link" %>
        </li>
       
      </ul>
    </div>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav"> 
        <% if current_user %>
        <li class="nav-item">
          <%= link_to current_user.name, user_profile_path(current_user.id), class:  "nav-link" %>
        </li>
        <li class="nav-item">
          <%= button_to "Logout", user_logout_path, method: :delete %>
        </li>
          
        <% else %>
        <li class="nav-item">
          <%= link_to "Sign Up", new_user_path %>
        </li>
        <li class="nav-item">
          <%= link_to "Login", user_login_path %>
        </li>
        <% end %>            
      </ul>
    </div>
  </div>
</nav>

